apiVersion: v1
kind: Pod #Pod must be starts with Capital "P" not p
metadata:
  name: service-demo
  labels: #Pod labels must match the Service selector labels to register with the Service.
    project: roboshop
    component: frontend
    environment: dev
spec:
  containers:
  - name: nginx #when it is list have to stert with -
    image: nginx
