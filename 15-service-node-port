apiVersion: v1
kind: Service
metadata:
  name: nginx-np #DNS name
spec:
  type: NodePort
  selector:
    project: roboshop
    component: frontend
    environment: dev
    purpose: node-port
  ports:
  - protocol: TCP
    port: 80        # Service port
    targetPort: 80 # Pod container port
   
